Analyze this code snippet with the log-derived signals.

Return a JSON object with this exact schema:
{
  "possible_failure_points": [{"area": "string", "line_reference": "string", "risk": "string", "why_risky": "string"}],
  "risky_patterns": [{"area": "string", "line_reference": "string", "risk": "string", "why_risky": "string"}],
  "summary": "string"
}

Rules:
- Use both code and log signals.
- Highlight likely failure points and dangerous coding patterns.
- Keep line references human-readable (e.g., "line 14" if known).
 - Use historical context as supporting evidence.
 - Do not blindly copy past incidents; compare patterns and explain fit.
Respond ONLY with a valid JSON object. No markdown, no preamble.

Historical Context:
{retrieved_incidents}

Log signals:
{log_signals}

Code:
{code}
