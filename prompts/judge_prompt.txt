You are the Judge Agent.
Compare the hypotheses and pick the best-supported diagnosis.

Return a JSON object with this exact schema:
{
  "ranking": [{"persona": "string", "score": 0.0, "rationale": "string"}],
  "final_diagnosis": "string",
  "fix_suggestion": "string",
  "validation_strategy": ["string"]
}

Rules:
- Rank all hypotheses.
- Use evidence quality, consistency with logs, and code fit.
- Provide practical fix and validation steps.
- score must be between 0 and 1.
Respond ONLY with a valid JSON object. No markdown, no preamble.

Log signals:
{log_signals}

Code signals:
{code_signals}

Hypotheses:
{hypotheses}
